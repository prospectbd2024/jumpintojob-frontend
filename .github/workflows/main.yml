name: Windows Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Debug
        run: |
          echo "Current directory: $(pwd)"
          ls -la C:\Users\prospectbddev\Desktop
      - name: Install dependencies (Optional)
        run: |
          cd C:\Users\prospectbddev\Desktop\jumpintojob-frontend
          git pull origin
          docker compose down
          docker compose up -d  --build --remove-orphans
          ./bin/deploy_dev.sh

